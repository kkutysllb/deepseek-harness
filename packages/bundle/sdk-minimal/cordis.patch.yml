# Standalone minimal SDK application. Unlike the ordinary SDK profile, this
# bundle does not layer over qilin-base: this insert is the complete Cordis tree.
# User profile, home, and invocation patches still apply above it.

- insert:
    - id: sdk-app-startup
      name: '@qilin/sdk-app'
      config:
        profile: sdk-minimal

    - id: sdk-jsonrpc-server
      name: '@qilin/sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      config:
        maxTokensAsSuccess: false

    - id: deepseek-llm-api-extensions
      name: '@qilin/deepseek-llm-api-extensions'

    - id: session-log-deepseek
      name: '@qilin/session-log-deepseek'

    - id: plugin-package-inventory-deepseek
      name: '@qilin/plugin-package-inventory-deepseek'

    - id: llm-deepseek
      name: '@qilin/llm-deepseek'
      config:
        apiKeyEnv: DEEPSEEK_API_KEY
        defaultContextWindow: !!js Number(process.env.OPENKYLIN_CONTEXT_WINDOW ?? 1000000)
        streamIdleTimeoutMs: 172800000

    - id: sandbox
      name: '@qilin/sandbox-local'

    # Shared projection registry: sandbox-policy and terminal-bash fold
    # sandbox-mode state through its units and require it as a hard injection.
    - id: session-projection
      name: '@qilin/session-projection'

    - id: sandbox-policy
      name: '@qilin/sandbox-policy'
      config:
        mode: danger-full-access
        workspaceRoot: !!js process.cwd()

    - id: subprocess
      name: '@qilin/subprocess-local'

    - id: pty
      name: '@qilin/terminal'

    - id: terminal-bash
      name: '@qilin/terminal-bash'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000

    - id: terminal-pwsh
      name: '@qilin/terminal-bash'
      disabled: !!js process.platform !== 'win32'
      config:
        shellDialect: pwsh
        timeoutMs: 300000

    # The editor uses the bare local filesystem; persistent Bash still consumes
    # the shared danger-full-access sandbox policy above.
    - id: fs-local
      name: '@qilin/fs-local'
      config:
        cwd: !!js process.cwd()

    # Keep the minimal agent kernel explicit: unlike qilin-base, this profile
    # owns each service row and omits every optional producer it does not use.
    - id: timer
      name: '@deepseek-ai/cordis-plugin-timer'

    - id: llm
      name: '@qilin/llm'

    - id: session
      name: '@qilin/session'

    - id: session-title
      name: '@qilin/session-title'
      config:
        fallbackMaxWords: 5
        fallbackMaxBytes: 40
        maxTitleBytes: 80

    - id: system-prompt
      name: '@qilin/system-prompt'
      config:
        includeHarnessIdentity: false
        includeRuntimeContext: false
        persona: !!js process.env.OPENKYLIN_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

    - id: tools
      name: '@qilin/tools'

    - id: agent
      name: '@qilin/agent'

    - id: llm-retry
      name: '@qilin/llm-retry'

    - id: jobs
      name: '@qilin/jobs-local'

    - id: invariants
      name: '@qilin/invariants'

    - id: session-invariant
      name: '@qilin/session/invariant'

    - id: agent-invariant
      name: '@qilin/agent/invariant'

    - id: scope-invariant
      name: '@qilin/scope/invariant'

    - id: agent-loop-invariant
      name: '@qilin/agent-loop/invariant'

    - id: agent-loop
      name: '@qilin/agent-loop'
      config:
        agents: []

    - id: persistent-bash
      name: '@qilin/tool-bash-persistent'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

    - id: persistent-pwsh
      name: '@qilin/tool-pwsh-persistent'
      disabled: !!js process.platform !== 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a PowerShell shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * State is persistent across command calls and discussions with the user.
          * Use native Windows paths (C:\...) and $env:NAME variables; this is PowerShell, not bash.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'Start-Job' or start a server with Start-Process.

    - id: str-replace-editor
      name: '@qilin/tool-str-replace-editor'
      config:
        maxOutputChars: 16000

    - id: sessions
      name: '@qilin/session-persistence-jsonl'
      config:
        root: !!js dshHomePath('sessions')
        compression: none
