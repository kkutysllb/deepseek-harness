/** Host loader entry for the account surface browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the account surface. */
export function apply(): void {}
