[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "openkylin-sdk"
version = "0.0.0.dev0"
description = "Python SDK for DeepSeek Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "DeepSeek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "openkylin-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/kkutysllb/OpenKylin"
Documentation = "https://github.com/kkutysllb/OpenKylin/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/kkutysllb/OpenKylin/issues"
Source = "https://github.com/kkutysllb/OpenKylin"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/openkylin_sdk"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
openkylin-runtime-bin = { path = "../sdk-runtime", editable = true }
