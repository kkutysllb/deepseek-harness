# Test-only composition: session-telemetry-otel through the real Loader/app
# path, exporting to the mock OTLP collector the driver starts (url via env).
# The redact-rule entry models a deployment mounting its own scrub rule on the
# session-telemetry/record waterfall — the seam itself ships no rules.
- id: logger-console
  name: '@deepseek-ai/cordis-plugin-logger-console'
  config:
    colors: false
    levels:
      default: 3
    showTime: ''

- id: cli-mock-llm
  name: './cli-mock-llm.ts'

- id: telemetry-redact-rule
  name: './telemetry-redact-rule.ts'

# Managed child-process groups required by the bash executor.
- id: subprocess
  name: '@qilin/subprocess-local'

- id: bash
  name: '@qilin/bash-local'

- id: session-telemetry-otel
  name: '@qilin/session-telemetry-otel'
  config:
    mode: !!js process.env.QILIN_TELEMETRY_E2E_MODE || 'FULL'
    exporter:
      url: !!js process.env.QILIN_TELEMETRY_E2E_URL

- id: agent-spine
  name: '@qilin/agent-spine-demo'
  config:
    agents:
      - id: main
        provider: cli-mock
        model: cli-mock
        cwd: !!js process.cwd()
    persona: 'Test the session-telemetry-otel plugin.'
    workspaceContext: false

- id: persistence
  name: '@qilin/session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: 'none'

- id: checkpoint-policy
  name: '@qilin/session-checkpoint-policy'
