# Add Code Mode and Cordis tools to the headless spawn/workflow stack.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: agent-spine
        name: '@qilin/agent-spine-demo'
        config:
          agents:
            - id: main
              provider: deepseek-official
              model: deepseek-v4-pro
              cwd: !!js process.cwd()
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are headless-agent, a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - id: persistence
        name: '@qilin/session-persistence-jsonl'
        config:
          root: './.sessions'
          compression: !!js "process.env.QILIN_SNAPSHOT === undefined ? 'zstd' : 'none'"
      - insert:
          - id: code-runtime
            name: '@qilin/code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@qilin/cordis-host-runner'
          - id: tool-cordis
            name: '@qilin/tool-cordis'
