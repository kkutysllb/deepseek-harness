# Keyless filesystem snapshots combine spill settings with replay. The
# sandboxed filesystem stack already lives in `qilin-base`; the ACP row re-pins
# `deepseek-v4-flash` for the recorded corpus.
- id: llm-deepseek
  name: '@qilin/llm-deepseek'
  disabled: true

- id: agent-default-model
  name: '@qilin/agent-default-model'
  config:
    provider: deepseek-official
    model: deepseek-v4-flash

- id: session-persistence-jsonl
  name: '@qilin/session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    compression: none

- id: agent-instructions
  name: '@qilin/agent-instructions'
  config:
    maxBytes: 65536

- id: system-prompt
  name: '@qilin/system-prompt'
  config:
    persona: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- insert:
    - id: llm-replay
      name: '@qilin/llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

- id: spill-local
  name: '@qilin/spill-local'
  config:
    root: !!js process.env.OPENKYLIN_SNAPSHOT_SPILL_ROOT ?? './.spill'

- id: spill-policy
  name: '@qilin/spill-policy'
  config:
    maxInlineBytes: 800
