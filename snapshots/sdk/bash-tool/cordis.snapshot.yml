# Keyless replay layer for the ordinary SDK snapshot composition.

- id: llm-deepseek
  name: '@qilin/llm-deepseek'
  disabled: true

- insert:
    - id: llm-replay
      name: '@qilin/llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
