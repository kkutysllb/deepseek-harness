# Keyless generic ACP v1 control-surface patch over `openkylin --profile acp`.

- id: llm-deepseek
  name: '@qilin/llm-deepseek'
  disabled: true

- id: acp
  name: '@qilin/acp'
  config:
    provider: control-fixture
    model: alpha

- id: session-persistence-jsonl
  name: '@qilin/session-persistence-jsonl'
  config:
    root: !!js process.env.OPENKYLIN_CONFORMANCE_PERSISTENCE_ROOT
    compression: none

- insert:
    - id: control-surface-llm
      name: './control-surface-llm.ts'
